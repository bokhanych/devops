version: "3"

services:

  nginx:
    build: ./nginx/
    ports:
      - 443:443
    volumes:
      - ./nginx/logs:/opt/nginx/logs

  apache:
    build: ./apache/
    ports:
      - 80:80
    volumes:
      - ./apache/logs:/opt/apache/logs      